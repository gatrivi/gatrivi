import {getApps,initializeApp} from 'firebase/app'; import {getFirestore,collection,getDocs,query,where,writeBatch,doc} from 'firebase/firestore'; import type {CrmData} from '../types';
const config={apiKey:import.meta.env.VITE_FIREBASE_API_KEY,projectId:import.meta.env.VITE_FIREBASE_PROJECT_ID,appId:import.meta.env.VITE_FIREBASE_APP_ID};
const app=getApps()[0]??initializeApp(config); const db=getFirestore(app); const names=['contacts','stages','deals','tasks'] as const;
export const firestoreRepository={async load(tenant:string):Promise<CrmData>{const result=await Promise.all(names.map(async name=>{const snap=await getDocs(query(collection(db,name),where('tenantId','==',tenant)));return snap.docs.map(item=>({id:item.id,...item.data()}));}));return {contacts:result[0] as CrmData['contacts'],stages:result[1] as CrmData['stages'],deals:result[2] as CrmData['deals'],tasks:result[3] as CrmData['tasks']};},async save(tenant:string,data:CrmData){const batch=writeBatch(db); for(const name of names)for(const item of data[name])batch.set(doc(db,name,item.id),item); await batch.commit();}};
